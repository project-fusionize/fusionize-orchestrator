import org.springframework.boot.gradle.plugin.SpringBootPlugin

plugins {
    id 'java'
    id 'org.springframework.boot' version "${springBootVersion}" apply false
    id 'io.spring.dependency-management' version "${springBootDependencyMangementVersion}"
}

dependencyManagement {
    imports {
        mavenBom SpringBootPlugin.BOM_COORDINATES
        mavenBom "org.springframework.ai:spring-ai-bom:${springAiVersion}"
    }
}

dependencies {
    testImplementation project(':fusionize:module:lib-common')
    testImplementation project(':fusionize:module:lib-common-test')
    testImplementation project(':fusionize:module:lib-user')
    testImplementation project(':fusionize:module:lib-workflow')
    testImplementation project(':fusionize:module:lib-workflow-components')
    testImplementation project(':fusionize:module:lib-workflow-orchestration')
    testImplementation project(':fusionize:module:lib-process')
    testImplementation project(':fusionize:module:lib-storage')

    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    testImplementation 'org.springframework.boot:spring-boot-starter-data-mongodb'
}
